---
# Variables specifiques au groupe bastion (override de all.yml si besoin)

# === Tailscale (uniquement sur bastion, edge n'en a pas) ===
tailscale_hostname: "monerometrics-bastion-poc"
tailscale_tags: "tag:bastion,tag:poc"
