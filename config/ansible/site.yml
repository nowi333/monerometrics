---

- name: Configure bastion (hardening CIS L1 + Tailscale)
  hosts: bastion
  gather_facts: true
  become: false
  roles:
    - common
    - tailscale

- name: Configure edge (hardening CIS L1 + nginx + Tor + TLS + WAF ModSecurity)
  hosts: edge
  gather_facts: true
  become: false
  roles:
    - common
    - nginx
    - tor
    - letsencrypt
    - modsecurity

- name: Configure k3s (hardening CIS L1 + data disk + Kubernetes single-node)
  hosts: k3s
  gather_facts: true
  become: false
  roles:
    - common
    - data_disk
    - k3s
