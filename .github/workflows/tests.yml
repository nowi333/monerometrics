name: tests

on:
  push:
    branches: [main]
    paths:
      - 'apps/worker/**'
      - 'apps/api/**'
      - 'apps/mcp/**'
      - 'infra/modules/network/**'
      - 'config/ansible/roles/letsencrypt/**'
      - 'apps/dashboard/src/**'
      - '.github/workflows/tests.yml'
  pull_request:
    paths:
      - 'apps/worker/**'
      - 'apps/api/**'
      - 'apps/mcp/**'
      - 'infra/modules/network/**'
      - 'config/ansible/roles/letsencrypt/**'
      - 'apps/dashboard/src/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      # Erreurs de syntaxe et noms indefinis seulement : ce qui plante au
      # lancement, pas une question de style.
      - run: pip install ruff && ruff check --select E9,F63,F7,F82 apps/api apps/worker apps/mcp
      - run: python scripts/ci/check_cloudflare_ips.py

  worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - run: pip install -r requirements-dev.txt
      - run: python -m pytest tests/ -q

  api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - run: pip install pytest
      - run: python -m pytest tests/ -q

  mcp:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mcp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - run: pip install -r requirements.txt pytest
      - run: python -m pytest tests/ -q

  dashboard:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/dashboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test 'src/**/*.test.js'
